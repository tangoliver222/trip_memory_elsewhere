{
  "name": "elsewhere-vanilla",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "build:hosting": "VITE_ELSEWHERE_API_BASE_URL= vite build --mode cloud",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/unit/*.test.js",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "firebase": "^12.16.0",
    "gsap": "^3.13.0",
    "lottie-web": "^5.13.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^7.0.0"
  }
}
